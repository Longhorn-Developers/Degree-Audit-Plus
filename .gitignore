# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts
claude.md

# Bun is the package manager; keep npm's lockfile out
package-lock.json
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude
*.code-workspace
.agents/
skills-lock.json

# docs/ is intentionally tracked — architecture.md and audit_scraping.md are
# versioned, and design docs are how decisions reach the team. It was ignored
# here, which silently dropped new files (the DAP-115 spike findings never
# committed because of this).

# Generated by tests/validate-parse-major.ts
tests/parse-major-test-results.txt
